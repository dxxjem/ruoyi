# 🚀 RuoYi-Vue-Plus 快速启动指南

## 推荐方式：使用批处理脚本（最简单）

在 `D:\test\ruoyi` 目录下，我已为您创建了 3 个启动脚本：

### 1️⃣ 启动后端
双击运行：**`START_BACKEND.bat`**
- 自动启动 Spring Boot 后端
- 端口：8080
- 配置：dev

### 2️⃣ 启动 SnailJob（可选）
双击运行：**`START_SNAILJOB.bat`**
- 启动任务调度服务器
- 端口：8800

### 3️⃣ 启动前端
双击运行：**`START_FRONTEND.bat`**
- 自动安装依赖（首次）
- 启动 Vue 前端开发服务器
- 端口：80

---

## 方式二：使用 VS Code 调试

### 前置条件
1. 安装 VS Code 扩展：**Extension Pack for Java**
2. 在 VS Code 中导入 Maven 项目：
   - 打开 `D:\test\ruoyi`
   - 等待 Java 扩展自动导入 Maven 项目
   - 右下角会提示 "Workspace is ready"

### 启动步骤
1. 打开 `D:\test\ruoyi` 目录
2. 按 `F5` 或点击左侧 "运行和调试"
3. 选择 **"🚀 RuoYi Backend (Maven)"**

---

## 方式三：使用命令行

### 启动后端
```bash
cd D:\test\ruoyi\RuoYi-Vue-Plus-v5.5.1
mvn spring-boot:run -pl ruoyi-admin
```

### 启动 SnailJob（新开终端）
```bash
cd D:\test\ruoyi\RuoYi-Vue-Plus-v5.5.1\ruoyi-extend\ruoyi-snailjob-server
mvn spring-boot:run
```

### 启动前端（新开终端）
```bash
cd D:\test\ruoyi\plus-ui-v5.5.1-v2.5.1
npm run dev
```

---

## 访问地址

启动成功后访问：
- 🌐 **前端应用**: http://localhost:80
- 🔧 **后端 API**: http://localhost:8080
- 📖 **接口文档**: http://localhost:8080/doc.html
- ⚙️ **SnailJob**: http://localhost:8800/snail-job

---

## 常见问题

### ❌ 问题1：找不到主类
**解决**：先执行编译
```bash
cd D:\test\ruoyi\RuoYi-Vue-Plus-v5.5.1
mvn clean compile -DskipTests
```

### ❌ 问题2：数据库连接失败
**解决**：检查 MySQL 是否启动，修改配置文件中的密码：
- `RuoYi-Vue-Plus-v5.5.1\ruoyi-admin\src\main\resources\application-dev.yml`
- `RuoYi-Vue-Plus-v5.5.1\ruoyi-extend\ruoyi-snailjob-server\src\main\resources\application-dev.yml`

默认配置：
```yaml
username: root
password: 123456
```

### ❌ 问题3：端口已被占用
**解决**：关闭占用端口的程序，或修改配置文件中的端口

---

## 启动顺序建议

1. ✅ 确保 MySQL 和 Redis 已启动
2. ✅ 启动后端（START_BACKEND.bat）
3. ✅ 等待看到 "Started DromaraApplication" 提示
4. ✅ 启动前端（START_FRONTEND.bat）
5. ✅ 浏览器访问 http://localhost:80

---

## 🎯 最推荐的方式

**直接双击批处理脚本！**
- `START_BACKEND.bat` - 启动后端
- `START_FRONTEND.bat` - 启动前端

简单快捷，无需复杂配置！
